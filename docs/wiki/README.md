# Wiki documentation moved

Product documentation for this package now lives in the **GitHub Wiki** for [Milestone-Consulting/Salesforce-Project-Management](https://github.com/Milestone-Consulting/Salesforce-Project-Management/wiki).

To edit pages locally, use the **`.wiki`** [git submodule](https://git-scm.com/book/en/v2/Git-Tools-Submodules) at the repository root (clone the main repo with `git clone --recurse-submodules`, or run `git submodule update --init .wiki` after clone).
